# Required — OpenAI API key for chat, embeddings, and fine-tuning.
OPENAI_API_KEY=sk-...

# Optional — fine-tuned Exit Advisor model id from tests/finetune_exit_advisor.ipynb.
# When unset, the Exit Advisor falls back to the base LLM used by the Main Agent.
EXIT_ADVISOR_MODEL=ft:gpt-4o-mini-...
